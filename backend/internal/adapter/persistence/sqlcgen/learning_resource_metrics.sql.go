// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: learning_resource_metrics.sql

package sqlcgen

import (
	"context"
)

const getLearningResourceMetricsByResourceIDs = `-- name: GetLearningResourceMetricsByResourceIDs :many
SELECT resource_id, like_count, save_count
FROM learning_resource_metrics
WHERE resource_id = ANY($1::bigint[])
`

// リソース一覧へlike_count/save_countを付与するためのまとめ取り。1件もいいね/保存が
// 無いリソースはlearning_resource_metrics行が存在しない（遅延生成）ため、この結果に
// 現れないリソースはGo側で0扱いにする（attachLearningResourceMetrics参照）。
func (q *Queries) GetLearningResourceMetricsByResourceIDs(ctx context.Context, dollar_1 []int64) ([]LearningResourceMetric, error) {
	rows, err := q.db.Query(ctx, getLearningResourceMetricsByResourceIDs, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LearningResourceMetric
	for rows.Next() {
		var i LearningResourceMetric
		if err := rows.Scan(&i.ResourceID, &i.LikeCount, &i.SaveCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const reconcileAllLearningResourceMetrics = `-- name: ReconcileAllLearningResourceMetrics :exec
INSERT INTO learning_resource_metrics (resource_id, like_count, save_count)
SELECT
    lr.id,
    COALESCE(l.cnt, 0),
    COALESCE(s.cnt, 0)
FROM learning_resources lr
LEFT JOIN (SELECT resource_id, COUNT(*) AS cnt FROM resource_likes GROUP BY resource_id) l ON l.resource_id = lr.id
LEFT JOIN (SELECT resource_id, COUNT(*) AS cnt FROM resource_saves GROUP BY resource_id) s ON s.resource_id = lr.id
ON CONFLICT (resource_id) DO UPDATE SET
    like_count = EXCLUDED.like_count,
    save_count = EXCLUDED.save_count
`

// 夜次reconcileジョブ本体。resource_likes/resource_savesの実件数から
// learning_resource_metrics全件をまとめて補正する。
func (q *Queries) ReconcileAllLearningResourceMetrics(ctx context.Context) error {
	_, err := q.db.Exec(ctx, reconcileAllLearningResourceMetrics)
	return err
}
